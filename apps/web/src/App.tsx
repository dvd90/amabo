/**
 * The device shell (docs/ARCHITECTURE.md §10). M0 placeholder: the molded
 * `<Device>`, the `<Amarium>` LCD, button-only nav, and the screens (Home, Status,
 * Feed, Clean, Play, Comfort, Journal, Sky, Lights-off) land at M8.
 */
export function App() {
  return (
    <main>
      <h1>Amabo</h1>
      <p>The Amarium is dark, waiting for the first Mote to condense.</p>
    </main>
  );
}
