{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install --frozen-lockfile && pnpm build"
  },
  "deploy": {
    "cronSchedule": "*/30 * * * *",
    "startCommand": "node apps/api/dist/cron/notify.js",
    "restartPolicyType": "NEVER"
  }
}
