node_modules
dist
build
coverage
*.tsbuildinfo
pnpm-lock.yaml

# Authored docs are the spec (the lore bible especially) — keep them verbatim,
# don't let a formatter reflow their tables and prose.
CLAUDE.md
docs/

# Generated Drizzle migration artifacts — owned by drizzle-kit, not the formatter.
apps/api/drizzle/

# Foundry submodules carry their own .prettierrc (and a solidity plugin we don't install);
# forge fmt owns the .sol files. Next's build output and the editor's local history: not ours.
packages/robinhood-contracts/lib/
apps/robinhood-web/.next/
.history/

# Written by forge (Deploy.s.sol) — generated, like the drizzle migrations.
packages/robinhood-contracts/deployments/
